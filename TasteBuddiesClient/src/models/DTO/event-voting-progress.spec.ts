import { EventVotingProgress } from './event-voting-progress';

describe('EventVotingProgress', () => {
  it('should create an instance', () => {
    expect(new EventVotingProgress()).toBeTruthy();
  });
});
