import { UserVotingProgress } from './user-voting-progress';

describe('UserVotingProgress', () => {
  it('should create an instance', () => {
    expect(new UserVotingProgress()).toBeTruthy();
  });
});
